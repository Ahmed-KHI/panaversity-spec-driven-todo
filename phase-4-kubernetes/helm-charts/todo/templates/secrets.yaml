{{/*
[Task]: T061
[From]: specs/004-phase-iv-kubernetes/tasks.md §Phase 9
[Description]: Secret template for reference (DO NOT use in production - use kubectl create secret instead)

IMPORTANT: This is a TEMPLATE file showing the structure of required secrets.
DO NOT commit real secrets to Git!

Create secrets manually using kubectl:

# Database secret:
kubectl create secret generic todo-database-secret \
  --from-literal=POSTGRES_PASSWORD=your-secure-password \
  --from-literal=DATABASE_URL=postgresql://todo_user:your-secure-password@todo-postgres:5432/todo_db

# OpenAI API key secret:
kubectl create secret generic todo-openai-secret \
  --from-literal=OPENAI_API_KEY=sk-your-openai-api-key

# Better Auth secret:
kubectl create secret generic todo-auth-secret \
  --from-literal=BETTER_AUTH_SECRET=$(openssl rand -base64 32) \
  --from-literal=BETTER_AUTH_URL=http://todo.local

*/}}

# Example Secret Structure (DO NOT USE IN PRODUCTION)
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.databaseSecret }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "todo.labels" . | nindent 4 }}
type: Opaque
# data:
#   POSTGRES_PASSWORD: <base64-encoded-password>
#   DATABASE_URL: <base64-encoded-connection-string>

---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.openaiSecret }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "todo.labels" . | nindent 4 }}
type: Opaque
# data:
#   OPENAI_API_KEY: <base64-encoded-api-key>

---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.authSecret }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "todo.labels" . | nindent 4 }}
type: Opaque
# data:
#   BETTER_AUTH_SECRET: <base64-encoded-secret>
#   BETTER_AUTH_URL: <base64-encoded-url>
